const funcao2Grau = (a,b,c) => {

    if (typeof x == 'undefined') x = 1;
    
    var f = a*(x*x) + b*x + c;
    console.log("Valor de a, b, c: " + a,b,c);
    console.log("Valor da Função(x): " + f);
  }

  funcao2Grau(1, 3, 5);
